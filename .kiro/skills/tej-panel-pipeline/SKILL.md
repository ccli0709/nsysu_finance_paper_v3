---
name: tej-panel-pipeline
description: >
  將 TEJ（台灣經濟新報）下載的多個 zip/CSV 財經資料庫，合併成年度追蹤資料
  (panel data)，並依序進行變數建構、樣本篩選、產業資料量統計與迴歸模型變數
  建立的一套可重複執行管線。本專案研究主題為「企業水管理與廢棄物管理（環境績效
  與資訊揭露）對成本黏性 (cost stickiness) 的影響」。當使用者要處理 TEJ 資料、做
  水資源／廢棄物／永續 (ESG) 與成本行為的實證研究、合併多份 TEJ CSV、或需要建立
  面板迴歸資料時使用。
  關鍵字：TEJ、台灣經濟新報、證券代碼、年月、panel data、面板資料、成本黏性、
  cost stickiness、ABJ、營業費用、SG&A、用水密集度、使用密集度、水回收率、
  水資訊揭露、廢棄物密集度、每百萬營收廢棄物、廢棄物揭露、廢棄物裁罰金額、
  裁處金額、Waste management、Water disclosure、TESG、SASB 主產業、TSE 產業、
  縮尾 winsorize、三重交乘、固定效果、產業異質性、時間落差。
---

# TEJ 面板資料處理管線（Panel Data Pipeline）

把 TEJ 下載的多份 CSV，一步步整併成可跑迴歸的研究樣本。程式以數字前綴命名，
**依序執行**即可；資料更新後，重新從 01 跑到最後一步即可重建所有輸出。

**研究主題**：以「**環境使用密集度**」（用水密集度＋廢棄物密集度）為**核心**，並以
水／廢棄物之**資訊揭露與違規裁罰**為輔，檢視其對**成本黏性 (cost stickiness)** 的影響，
以 TEJ 台灣製造業（排除金融保險業）為樣本。理論與模型參考 `reference_paper_main/` 兩篇
主要文獻（低碳政策／綠色創新、ESG 資訊揭露），並援引 `reference_paper_sub/` 多篇國內
碩博論文，詳見 `study.md`。

**假設**（H6 為核心）：
- 核心：**H6** 用水密集度（與廢棄物密集度）越高→黏性越大（β3<0），且效果集中於高污染產業。
- 廢棄物：H3 每百萬營收廢棄物越高→黏性越大（β3<0）；H4 揭露品質越高→黏性較低（β3>0）；
  H5 廢棄物裁罰金額越高→黏性越大（β3<0）。
- 水（輔助）：H1 水回收率越高→黏性越大（β3<0）；H2 有揭露→黏性較低（β3>0）。

所有模型均含**產業(SASB)與年份固定效果**及公司叢集穩健標準誤。

**目前主要結果**：樣本存在穩健成本黏性（β2<0）；核心之**用水密集度於高污染產業當期
β3≈−0.091\***（N≈8,484，樣本涵蓋度遠高於水回收率）、廢棄物密集度於高污染遞延期亦
顯著加劇；輔助之水回收率／揭露／裁罰調節效果不穩健。

## 執行順序

```
python 01_merge_tej_data.py          # 合併原始 zip/CSV → 01_merged_tej_data.csv
python 02_feature_engineering.py     # 變數建構        → 02_features_data.csv
python 03_sample_selection.py        # 樣本篩選+產業統計→ 03_sample_data.csv, 03_industry_stats.csv
python 04_build_model_variables.py   # 交乘項+遞延+分組 → 04_model_data.csv, 04_var_mapping.csv, 04_industry_wateruse.csv
python 05_run_regressions.py         # 主模型+時間落差   → 05_regression_results.txt, 05_regression_coef.csv
python 06_robustness_checks.py       # 產業異質性×遞延   → 06_robustness_results.txt, 06_robustness_coef.csv, 06_robustness_summary.csv
python 07_generate_paper.py          # 生成 Word 論文    → 水與廢棄物管理與成本黏性_論文.docx
python 08_generate_thesis_template.py # 生成中山格式論文  → 水與廢棄物管理與成本黏性_論文_中山格式.docx
python append_desc_table.py          # 於中山格式文末附加樣版格式「表4-3 敘述統計」
```

需額外套件：`statsmodels`（OLS + 產業/年份固定效果 + 叢集穩健標準誤）、
`pypdf` 與 `cryptography`（讀參考論文，含 AES 加密之 PDF）、`python-docx`（產出 Word）。
安裝：`python -m pip install statsmodels pypdf cryptography python-docx`

命名慣例：`NN_描述.py` 產出 `NN_描述.csv`；下一步的輸入 = 上一步的輸出，
方便日後資料更新時「從頭依序重跑」。新增步驟就往後接 `05_`、`06_`。

## 環境

- Python 3.x + pandas、numpy。
- 原始資料放在 `tej_data/`，為 TEJ 匯出的 zip（內含單一 CSV）。

## 原始資料集（tej_data/ 內的多個 zip）

| zip 前綴 | 頻率 | 主鍵欄 | 主要欄位 | 對應 TEJ 路徑 |
|---|---|---|---|---|
| `TEJ20260723012741` | **單季**財務 (年月 MM=03/06/09/12) | 證券代碼＋年月 | 營業費用、推銷費用、管理費用、研究發展費、資產總額、員工人數-母公司、ROA(C)稅前息前折舊前、ROA(A)稅後息前、負債比率、總負債/總淨值、營業收入淨額 | IFRS 以合併為主財務(單季)-一般產業Ⅳ |
| `TEJ20260723014020` | **年度** (年) | 證券代碼＋年＋序號 | 回收利用水量、製程水回收率%、水回收率%、總用水量(含回收水) | TESG 環境構面-企業用水量 |
| `TEJ20260723104157` | 年月 (MM=06/12) | 證券代碼＋年月 | E_用水回收率%、E_取得用水相關國際認證、E_GRI_用水及廢水管理揭露度、SASB主產業 | TESG ESG-變數彙總表 |
| `TEJ20260724105926` | **公司靜態** | 證券代碼 | TSE產業_代碼／名稱、TEJ產業_名稱、TEJ子產業_名稱 | TEJ Company DB-屬性基本資料 |
| `TEJ20260727061428` | 年月 (MM=06/12) | 證券代碼＋年月 | E_GRI_廢棄物管理揭露度、E_每百萬營收廢棄物、E_有害廢棄物回收百分比%、G_報廢產品再循環% | TESG ESG-廢棄物揭露 |
| `TEJ20260727062055` | **年度** (含自由文字，robust 讀取) | 證券代碼＋年 | 總重量(有害+非有害)、廢棄物密集度(公噸/單位)、有害/非有害廢棄物量、認證 | TESG-廢棄物量/密集度 |
| `TEJ20260727063019` | **年度** | 證券代碼＋年 | 一般廢棄物罰鍰次數、事業廢棄物罰鍰次數、廢棄物清除處理機構罰鍰次數 | TESG-廢棄物罰鍰 |
| `v5-20260801/TEJ20260801102736` | **明細**(robust) | 證券代碼＋裁處年份 | 裁處金額、污染類別、違反法令…（逐案裁處） | wcsr30a 列管事業污染源裁處資料 |

年份範圍：財務 2005–2026、水資源 2006–2025、ESG／廢棄物涵蓋各年；裁處明細 2010–2026；
產業別為公司靜態。`FINE_DETAIL_ZIP` 用於建構連續之「廢棄物裁罰金額」（篩污染類別含
「廢棄物」之裁處金額，依 [證券代碼, 裁處年份] 加總），取代原「罰鍰次數」。
含自由文字欄（如廢棄物量資料範圍）的檔以 `read_tej_zip(..., robust=True)`（python 引擎、
跳過欄數異常列）讀取，僅保留數值欄。

## 各步驟重點

### 01 合併（01_merge_tej_data.py）
- TEJ CSV 特性：**UTF-16 (LE, 含 BOM)、Tab 分隔**；「證券代碼」值形如
  `2201 裕隆`（代碼＋公司名稱，需以第一個空白拆開，只留代碼）。
- 面板頻率為**年度**，主鍵：**證券代碼 + 西元年份**。
- 三個資料集彙總為年度的方式不同（關鍵設計）：
  - **財務（單季）→ 年度**：年月前 4 碼為年份。
    - 流量變數（營業費用、推銷費用、管理費用、研究發展費、營業收入淨額）：
      **同年四季加總**為年度值（需該年四季齊全，否則標記缺值）。
    - 存量／比率變數（資產總額、員工人數-母公司、ROA、負債比率、總負債/總淨值）：
      **取年末 Q4（12 月）**該季值。
  - **水資源**：已是年度資料，直接以 [證券代碼, 年] 使用；若同年有多筆（序號），
    取有效值（非空）或首筆。
  - **ESG（年月 06/12）**：年月前 4 碼為年份，同年若有 06 與 12 兩筆，**優先取
    12（年末）**，否則取現有一筆。
- 合併：以 [證券代碼, 西元年份] 對財務與水資源做 outer join；ESG 的
  `SASB主產業` 視為公司靜態屬性（取各公司最新非空值），套用到該公司所有年份；
  ESG 的年度揭露欄（E_GRI_用水及廢水管理揭露度 等）以 [證券代碼, 年份] left join。
- 更新資料時：把新的 zip 檔名加入對應清單並註明頻率（單季/年度/年月）。

### 02 變數建構（02_feature_engineering.py）
延續成本黏性文獻（ABJ 模型）與 `study.md` 的變數設計：
1. 對每家公司依年份排序，計算前一年度值（t-1）。
2. **應變數與核心變數**：
   - ΔLNSGA = LN(本期營業費用) − LN(前期營業費用)。營業費用可直接取
     「營業費用」欄，或以「推銷費用＋管理費用」相加（提供參數切換）。
   - ΔLNREV = LN(本期營業收入淨額) − LN(前期營業收入淨額)。
   - D（收入下降虛擬變數）：本期營收 < 前期營收則為 1，否則 0。
3. **自變數（水管理）**：
   - `Water_Rate`（連續，主分析）：水回收率% 或 製程水回收率%（兩者皆保留，
     以參數擇一為主，另一作穩健性）。
   - `Water_Disc`（虛擬，次分析）：
     - 做法 A：該公司當年在水資源庫有「回收利用水量／水回收率」紀錄 = 1，否則 0。
     - 做法 B：ESG 的 E_GRI_用水及廢水管理揭露度 > 0 = 1，否則 0。
4. **控制變數**：
   - Size = LN(資產總額)。
   - AI（資產密集度）= 資產總額 / 營業收入淨額。
   - EI（員工密集度）= 員工人數-母公司 / 營業收入淨額。
   - ROA：取 ROA(A)稅後息前（或 ROA(C)，參數切換）。
   - Lev（財務槓桿）= 負債比率。
   - Decrease（連續兩年營收下降）：本期與前期皆 D=1 則為 1，否則 0。
5. 縮尾 Winsorize：對 ΔLNSGA、ΔLNREV、Water_Rate、AI、EI、ROA、Lev 等連續變數
   做前後 1%（`_w` 欄位），降低極端值影響。
6. **使用密集度（核心）與廢棄物管理變數**：
   - `Water_Intensity`（**核心**）＝總用水量(含回收水) / 營收（縮尾）——樣本涵蓋度高。
   - `Waste_Intensity`（核心／實質投入）＝ E_每百萬營收廢棄物（縮尾）。
   - `Waste_Disc`（輔助／資訊透明度）＝ E_GRI_廢棄物管理揭露度（連續揭露品質）。
   - `Waste_Fine`（輔助／風險衝擊）＝ **廢棄物裁罰金額佔資產**（廢棄物裁罰金額 / 資產，
     縮尾；取代原罰鍰次數，避免 366 極端值主導）；另存 `Waste_FineInt`。
   - 補充 `Waste_Density`＝廢棄物密集度(公噸/單位)。
   - 連續變數（含 Water_Intensity、Waste_Intensity、Waste_FineInt）皆做前後 1% 縮尾。
7. **遞延期變數（方向四：時間落差）**：不新增概念變數，僅將既有變數
   （Water_Rate_w、製程水回收率%_w、Water_Disc、Water_Disc_GRI、Waste_Intensity_w、
   Waste_Disc、Waste_FineInt_w、Water_Intensity_w）平移 t-1、t-2（`_l1`／`_l2`），
   且僅在年份差恰為 k 時有效。

### 03 樣本篩選 + 產業統計（03_sample_selection.py）
- 篩選（參數在檔案上方）：
  - 限定研究期間 `START_YEAR`~`END_YEAR`。
  - **排除金融保險業**（依 SASB主產業／交易所產業名為「金融」者剔除）。
  - 剔除 ΔLNSGA、ΔLNREV、D 任一缺值（迴歸必要欄）。
  - 剔除營收或營業費用 ≤ 0（無法取對數）者。
- 產出 `03_industry_stats.csv`：各產業（SASB主產業）的公司數、觀測數、
  有水資料筆數（Water_Rate 非空）、有揭露筆數（Water_Disc=1）、
  迴歸可用筆數，**用來檢視各產業樣本充足度與揭露比例**。

### 04 建模變數（04_build_model_variables.py）
- 建立成本黏性模型所需的**交乘項**（`Env_Var` 泛指環境變數）：
  - `D×ΔLNREV`（捕捉整體成本黏性，係數 β2 預期為負）。
  - `Env_Var×D×ΔLNREV`（三重交乘，核心係數 β3）：核心代入 `Water_Intensity`（用水密集度）
    與 `Waste_Intensity`；輔助代入 `Water_Rate`、`Water_Disc`、`Waste_Disc`、`Waste_Fine`
    （裁罰金額佔資產）。
  - **遞延交乘（方向四）**：`Env_Var(t-1)×D×ΔLNREV`、`Env_Var(t-2)×D×ΔLNREV`
    （欄名 `*_D_dREV_l1`／`_l2`），檢驗環境投入之遞延效益。
  - 各控制變數與 `D×ΔLNREV` 的交乘（Controls×D×ΔLNREV），依 ABJ 慣例納入。
- **高／低耗水產業分組（方向五）**：依 TEJ Company DB 之 `TSE產業_代碼`，以文獻／
  實務界定之用水密集製造業（半導體 M2324、光電 M2326、電子零組件 M2328、紡織
  M1400、造紙 M1900、鋼鐵 M2000、化學 M1721、水泥 M1100、玻璃陶瓷 M1800、食品
  M1200、塑膠 M1300、橡膠 M2100、油電燃氣 M9700）為 `WaterUse`＝高耗水，其餘為
  低耗水；另輸出 `04_industry_wateruse.csv`（各 TSE 產業觀測數與分組）。
- 產業（SASB主產業）與年份轉為虛擬變數群（固定效果）。
- 欄名含 `%`、空白、`×`、`Δ` 等字元會先安全化（如 `Water_Rate`、`D_x_dREV`），
  並保留原始名對照表，供後續 statsmodels 與論文對表使用。

### 05 實證迴歸（05_run_regressions.py）
- 方法：**OLS（pooled）+ 產業與年份固定效果（虛擬變數）+ 公司叢集穩健標準誤**
  （cluster by 證券代碼），為成本黏性文獻標準設定。
- 主模型（對應 study.md）：
  ```
  ΔLNSGA = β0 + β1·ΔLNREV + β2·(D×ΔLNREV) + β3·(Water_Var×D×ΔLNREV)
           + β4·Water_Var + Σ Controls×D×ΔLNREV + Σ Industry + Σ Year + ε
  ```
- 五組主結果，每組再跑**當期 / t-1 / t-2**（方向四）三個遞延期：
  - 模型 1（水·主分析）：Water_Var = `Water_Rate`（水回收率%）。
  - 模型 2（水·次分析）：Water_Var = `Water_Disc`（揭露虛擬變數）。
  - 模型 3（廢棄物·主分析 H3）：`Waste_Intensity`（每百萬營收廢棄物）。
  - 模型 4（廢棄物·次分析 H4）：`Waste_Disc`（廢棄物揭露品質）。
  - 模型 5（廢棄物·穩健 H5）：`Waste_Fine`＝**廢棄物裁罰金額佔資產**（取自 wcsr30a
    裁處明細之「裁處金額」，連續衡量取代原罰鍰次數，解決 366 極端值）。
  - 模型 6（**核心 H6**）：`Water_Intensity`＝用水密集度（總用水量/營收）。
- **研究主軸**：以「使用密集度」（用水密集度＋廢棄物密集度）為核心、揭露與裁罰為輔；
  用水密集度樣本 ~8,484，於高污染產業當期 β3≈−0.091** 顯著（最強結果）。
- 係數解讀：
  - β2 < 0 且顯著 → 樣本存在成本黏性。
  - β3（H6 核心）：`Water_Intensity`／`Waste_Intensity` 之 β3 顯著為**負** →
    使用密集度越高、營收下降時成本越難削減，**加劇**黏性；全樣本當期多不顯著，
    顯著性主要顯現於高污染產業子樣本（見 06）。
  - β3（H1/H3/H5）預期為負（加劇）、β3（H2/H4）預期為正（緩解）。
  - 遞延期若在 t-1／t-2 才顯著 → 支持環境投入之僵固性需時間發酵。
- 結果檔附遞延期 β3 對照；N 取自實際迴歸樣本（去除任一缺值後）。

### 06 穩健性檢定（06_robustness_checks.py）
- 輸入改讀 `04_model_data.csv`（已含遞延交乘與 `WaterUse` 分組）。
- **衡量 × 產業組 × 遞延期交叉**：對每個組合重跑主模型，輸出
  `06_robustness_summary.csv`（6 衡量 × 3 產業組 × 3 遞延期 = 54 設定）：
  1. **衡量**：用水密集度、水回收率%、水揭露、廢棄物密集度、廢棄物揭露、廢棄物裁罰金額。
  2. **產業組**：全樣本 / 高耗水（高污染）/ 低耗水。
  3. **遞延期**：t-0（當期）/ t-1 / t-2。
- 目的：比較高／低污染產業與各遞延期下 β3 的方向與顯著性，檢驗「效果集中於高污染
  產業且具遞延性」之推論（核心結果：用水密集度·高污染·當期 β3≈−0.091**）。

### 07 生成 Word 論文（07_generate_paper.py）
- 架構：中文摘要 / Abstract / 第一章 緒論（背景動機、研究目的，約 3,000 字）/
  第二章 文獻探討（第一節 文獻回顧約 3,000 字 → H1、H2 水；H3–H5 廢棄物；
  **H6 使用密集度核心**；近年國內 ESG 與研究缺口）/ 第三章 研究方法 /
  第四章 實證結果（敘述統計、相關矩陣、**核心：使用密集度**當期與遞延、
  **輔助：揭露與裁罰**、產業異質性 × 遞延期，核心／輔助分表呈現）/
  第五章 結論與建議（含政策與研究貢獻）/ 參考文獻。
- **以使用密集度為核心、揭露與裁罰為輔**；主迴歸六模型（模型6 用水密集度、
  模型3 廢棄物密集度為核心；模型1/2/4/5 為輔助）。
- 通用表格建構：`build_models_table(coef, specs)`、參數化 `build_lag_table(kind_cols)`。
- `summarize_findings` 自動掃描 β3 顯著性，依實際結果自適應生成摘要／結論句。
- **參考文獻採統一 [n] 編號**：[1]–[3] 主要文獻（`reference_paper_main/`）、
  [4]… 國內碩博論文（`reference_paper_sub/`，依年份新到舊）；`CITE_NUM`＋`cmark()`
  於每段文末附對應標號。敘述統計、相關矩陣由 `04_model_data.csv` 即時計算；
  迴歸表讀 `05_regression_coef.csv`／`06_robustness_summary.csv`。
- 輸出檔：`水與廢棄物管理與成本黏性_論文.docx`。

### 08 生成中山格式論文（08_generate_thesis_template.py）＋ append_desc_table.py
- 以 `template_paper/` 之碩論樣版格式（標楷體、章名不加「第X章」、三線表：係數＋
  下一列括號 t 值）產出 `水與廢棄物管理與成本黏性_論文_中山格式.docx`。
- 內容與 07 一致（使用密集度核心、六模型、H6、裁罰金額、統一 [n] 編號）。
- `append_desc_table.py`：於文末附加與樣版「表4-3 敘述統計」相同格式之三線表
  （8 欄：變數＋Mean/SD/Min/P25/Median/P75/Max，數值靠右、sz=8 三線）。

## 已知資料特性 / 注意事項

- **頻率不一致**：財務為單季、水資源為年度、ESG 為 06/12 年月；01 步驟統一彙總為
  年度是整條管線的關鍵前提（流量加總、存量取年末）。
- **產業別來源**：固定效果用 ESG 之 `SASB主產業`（公司靜態）；高／低污染分組用
  TEJ Company DB 之 `TSE產業_代碼`（見 04）。
- **衡量選擇（重要）**：水構面以**用水密集度**（總用水量/營收，樣本 ~8,000+）為主，
  水回收率%（樣本 ~1,000、變異低）僅作輔助；廢棄物違規以**裁罰金額佔資產**（連續）
  取代罰鍰次數（離散、易受單一極端值 366 主導），以兼顧衡量效度與檢定力。
- **GDP 成長率等總體控制**：不在 TEJ zip 內；年份固定效果已吸收年度總體效果，
  故可不另外納入外部總體變數（如需仍可外掛年度 GDP）。
- 取對數前需確保營業費用、營業收入淨額為正值；負值或零值於 03 剔除。

## 換主題 / 換資料時如何沿用

1. 把新的 TEJ zip 丟進 `tej_data/`，在 01 登記檔名並註明頻率（單季/年度/年月）與
   欄位；單季財務記得設定流量欄（加總）與存量欄（取年末）清單。
2. 若主鍵欄名不同（只有「年」或「年月」），01 已自動判斷年份擷取方式。
3. 調整 02 的變數建構清單（應變數、水管理衡量、控制變數）、03 的篩選參數
   （期間、排除產業）、04 的交乘項與固定效果設定、05/06 的模型與穩健性維度。
4. 從 `01` 依序重跑到最後一步。
